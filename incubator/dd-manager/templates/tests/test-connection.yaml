apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dd-manager.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "dd-manager.name" . }}
    helm.sh/chart: {{ include "dd-manager.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "dd-manager.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
